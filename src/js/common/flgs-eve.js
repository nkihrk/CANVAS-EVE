/**
 *
 * Global flags for CANVAS EVE.
 *
 * Dependencies
 * - jQuery 3.4.1
 *
 */

import $ from 'jquery';

const FlgsEve = (function(w, d) {
  function Flgs() {}

  Flgs.prototype = {
    constructor: Flgs
  };

  return Flgs;
})(window, document);

export default FlgsEve;
